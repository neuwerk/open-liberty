<?xml version="1.0"?>
<!DOCTYPE project [
        <!ENTITY instance SYSTEM "../../../instance.xml"><!-- file:34 -->
        <!ENTITY properties SYSTEM "../../xmls/properties.xml"><!-- file:36 -->
        <!ENTITY targets SYSTEM "../../xmls/targets.xml"><!-- file:33 -->
        <!ENTITY taskdefs SYSTEM "../../xmls/taskdefs.xml"><!-- file:34 -->
        <!ENTITY taskdefs_post_compile SYSTEM "../../xmls/taskdefs_post_compile.xml"><!-- file:47 -->
        <!ENTITY path_refs SYSTEM "../../xmls/path_refs.xml"><!-- file:35 -->
]>

<!--
   1.5, 8/3/07

   Description: This is the install file for the wasintegration/ejb test.

   Change History:
   Date        UserId         Feature/Defect          Description
   06/20/2006  btiffany       LIDB3261.31.01          New File
-->

<project default="install" basedir=".">


  <!-- These are XML entity refs.  Basically these statements cause the
       files declared in the DOCTYPE at the top of the file to be imported
       into this build file.
  -->
  &instance;
  &properties;
  &path_refs;
  &taskdefs;
  &taskdefs_post_compile;
  &targets;

  <!-- The install target will install the EAR or WAR file into WebSphere.
  -->
  <target name="testit" depends="setenv">
    <InstallApp earName="websvcanno.ear"
                appName="websvcanno"
                localInstall="${conn.type}"
                deployEjb="true"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>
  </target>


  <target name="install" depends="setenv">

    <InstallApp earName="websvcanno.ear"
                appName="websvcanno"
                localInstall="${conn.type}"
                deployEjb="true"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>

    <InstallApp earName="websvcannoIf.ear"
                appName="websvcannoIf"
                localInstall="${conn.type}"
                deployEjb="true"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>


    <InstallApp earName="websvcanno11.ear"
                appName="websvcanno11"
                localInstall="${conn.type}"
                deployEjb="true"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>

    <!--
    <InstallApp earName="websvcanno2.ear"
                appName="websvcanno2"
                localInstall="${conn.type}"
                deployEjb="true"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>
    -->

    <InstallApp earName="websvcanno3.ear"
                appName="websvcanno3"
                localInstall="${conn.type}"
                deployEjb="true"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>


    <!--
    <InstallApp earName="websvcanno4.ear"
                appName="websvcanno4"
                localInstall="${conn.type}"
                deployEjb="true"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>
    -->

    <InstallApp earName="websvcanno5.ear"
                appName="websvcanno5"
                localInstall="${conn.type}"
                deployEjb="true"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>

    <InstallApp earName="websvcanno6.ear"
                appName="websvcanno6"
                localInstall="${conn.type}"
                deployEjb="true"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>

    <InstallApp earName="websvcanno9.ear"
                appName="websvcanno9"
                localInstall="${conn.type}"
                deployEjb="true"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>

    <InstallApp earName="websvcannob.ear"
                appName="websvcannob"
                localInstall="${conn.type}"
                deployEjb="true"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>



  </target>

</project>

