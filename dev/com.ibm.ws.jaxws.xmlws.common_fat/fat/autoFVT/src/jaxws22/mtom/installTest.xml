<?xml version="1.0"?>                     
<!--
 
   Description:  install script for jaxws22 tests
   Change History:
   
   Date       UserId      Defect          Description
   03/03/10   btiffany                    New File
-->

<project default="install" basedir=".">

  <!-- use import instead of entity refs -->   
  <import file="../../xmls/common_imports.xml"/>

  <!-- The install target will install the EAR or WAR file into WebSphere.
  -->
  <target name="install" depends="setenv">
 
    <InstallApp earName="jaxws22mtom_jsr109v13.ear"
                appName="jaxws22mtom_jsr109v13"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.base.dir}/src/jaxws22/mtom/etc/apps"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>
    
    
    
    
   </target>
  
</project>

