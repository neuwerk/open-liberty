<?xml version="1.0"?>

<!--
   autoFVT/src/jaxwsejb30/ejbapps/depdesc/webxml/installTest.xml, WAS.websvcs.fvt, WAS85.FVT, cf011231.01
   
   Change History:
   Date       UserId              Feature/Defect          Description
   ***************************************************************************
   03/11/2008 samerrel            LIDB4511.45.01          new file
   07/07/2008 samerrel            534715                  Formatting fixes
-->

<project default="install" basedir=".">
    <import file="../../../../xmls/common_imports.xml" />
    <import file="buildTest.xml" />

    <target name="install" depends="setenv">
        <InstallApp earName="${ejbsvc.ear}.ear"
                    appName="${ejbsvc.ear}"
                    localInstall="${conn.type}"
                    useDefaultBindings="true"
                    installRootLocation="${FVT.build.installableApps.dir}"
                    installMode="${install.mode}"
                    fvtRootLocation="${FVT.base.dir}">
        </InstallApp>
        <InstallApp earName="${warsvc.ear}.ear"
                    appName="${warsvc.ear}"
                    localInstall="${conn.type}"
                    useDefaultBindings="true"
                    installRootLocation="${FVT.build.installableApps.dir}"
                    installMode="${install.mode}"
                    fvtRootLocation="${FVT.base.dir}">
        </InstallApp>
        <InstallApp earName="${warref.ear}.ear"
                    appName="${warref.ear}"
                    localInstall="${conn.type}"
                    useDefaultBindings="true"
                    installRootLocation="${FVT.build.installableApps.dir}"
                    installMode="${install.mode}"
                    fvtRootLocation="${FVT.base.dir}">
        </InstallApp>
    </target>
</project>
