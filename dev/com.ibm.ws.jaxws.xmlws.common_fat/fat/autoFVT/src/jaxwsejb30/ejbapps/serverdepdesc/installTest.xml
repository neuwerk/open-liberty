<?xml version="1.0"?>

<!--
    autoFVT/src/jaxwsejb30/ejbapps/serverdepdesc/installTest.xml, WAS.websvcs.fvt, WAS85.FVT, cf011231.01
    
    Change History:
    Date       UserId              Feature/Defect          Description
    ****************************************************************************
    06/28/2008 samerrel            LIDB4511.45.01          New File
    
-->

<project default="install" basedir=".">
    <import file="../../..//xmls/common_imports.xml" />
    <import file="buildTest.xml" />

    <target name="install" depends="setenv">
        <InstallApp earName="${pkg.name.echoejb}.ear"
                    appName="${pkg.name.echoejb}"
                    localInstall="${conn.type}"
                    useDefaultBindings="true"
                    installRootLocation="${FVT.build.installableApps.dir}"
                    installMode="${install.mode}"
                    fvtRootLocation="${FVT.base.dir}">
        </InstallApp>
        <InstallApp earname="${pkg.name.echowar}.ear"
                    appName="${pkg.name.echowar}"
                    localInstall="${conn.type}"
                    useDefaultBindings="true"
                    installRootLocation="${FVT.build.installableApps.dir}"
                    installMode="${install.mode}"
                    fvtRootLocation="${FVT.base.dir}">
        </InstallApp>
    </target>

</project>
