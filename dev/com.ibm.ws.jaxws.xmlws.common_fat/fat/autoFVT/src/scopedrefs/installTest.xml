<?xml version="1.0"?>                     
<!--
 
   Description:  install script 
   Change History:
   
   Date       UserId      Defect          Description
   10/05/10   btiffany                    New file
-->

<project default="install" basedir=".">

  <!-- use import instead of entity refs -->   
  <import file="../xmls/common_imports.xml"/>

  <!-- The install target will install the EAR or WAR file into WebSphere.
  -->
  <target name="install" depends="setenv, installapp1">
   		 <InstallApp earName="scopedrefsapp2.ear"
                appName="scopedrefsapp2"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}" 
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
	    </InstallApp>
  </target>
   
   <target name="installapp1" depends="setenv">
	    <InstallApp earName="scopedrefsapp1.ear"
	                appName="scopedrefsapp1"
	                localInstall="${conn.type}"
	                useDefaultBindings="true"
	                installRootLocation="${FVT.build.installableApps.dir}" 
	                installMode="${install.mode}"
	                fvtRootLocation="${FVT.base.dir}">
	    </InstallApp>
  </target>
  
</project>

