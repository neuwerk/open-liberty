<?xml version="1.0"?>

<!--
    %I, 6/30/09
    
    Change History:
    Date       UserId              Feature/Defect          Description
    ****************************************************************************
    06/29/2009 samerrel            552420                  New File
    
-->

<project default="uninstall" basedir=".">
    <import file="../../xmls/common_imports.xml" />
    <import file="buildTest.xml" />

    <target name="uninstall" depends="setenv">
        <UninstallApp appName="${hellowar}"
                      fvtRootLocation="${FVT.base.dir}"
                      uninstallMode="${uninstall.mode}"
                      localInstall="${conn.type}">
        </UninstallApp>
        <UninstallApp appName="${helloejb}"
                      fvtRootLocation="${FVT.base.dir}"
                      uninstallMode="${uninstall.mode}"
                      localInstall="${conn.type}">
        </UninstallApp>
        <UninstallApp appName="${hellowarclient}"
                      fvtRootLocation="${FVT.base.dir}"
                      uninstallMode="${uninstall.mode}"
                      localInstall="${conn.type}">
        </UninstallApp>
        <UninstallApp appName="${helloejbclient}"
                      fvtRootLocation="${FVT.base.dir}"
                      uninstallMode="${uninstall.mode}"
                      localInstall="${conn.type}">
        </UninstallApp>
    </target>
</project>
