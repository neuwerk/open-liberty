<?xml version="1.0" encoding="UTF-8"?>                 

<!-- 
  @(#) 1.1 WautoFVT/src/jaxws/badproxy/installTest.xml, WAS.websvcs.fvt, WSFP.WFVT 9/27/06 09:03:32 [11/3/06 09:02:07]

  Description: install file for the JAX-WS BadProxy tests
 
  Date        Author       Feature/Defect          Description
  09/27/06    sedov        LIDB3296.41-02          New File
-->

<project default="install" basedir=".">
   
	<property name="componentName" value="jaxws/badproxy"/>
	<property name="test.name" value="jaxws.badproxy" />
	
    <import file="..\jaxws-commons.xml" />

	<property name="test.base.dir" value="${FVT.base.dir}/src/${componentName}" />

  <!-- The install target will install the EAR or WAR file into WebSphere.
  -->
  <target name="install" depends="setenv">
   <echo message="Installing ${test.name}App.ear ..." />
  	
    <InstallApp earName="${test.name}.loggerApp.ear"
                appName="${test.name}.logger"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>

    <InstallApp earName="${test.name}.MismatchedSEIApp.ear"
                appName="${test.name}.MismatchedSEI"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>
	
    <InstallApp earName="${test.name}.MismatchedSOAP11App.ear"
                appName="${test.name}.MismatchedSOAP11"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>    	
  	
    <InstallApp earName="${test.name}.MismatchedSOAP12App.ear"
                appName="${test.name}.MismatchedSOAP12"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>  

    <InstallApp earName="${test.name}.MismatchedWsdlApp.ear"
                appName="${test.name}.MismatchedWsdl"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>  
	
    <InstallApp earName="${test.name}.NoWsdlHTTPApp.ear"
                appName="${test.name}.NoWsdlHTTP"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>      	
  	
    <InstallApp earName="${test.name}.NoWsdlSOAP11App.ear"
                appName="${test.name}.NoWsdlSOAP11"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>    

    <InstallApp earName="${test.name}.NoWsdlSOAP12App.ear"
                appName="${test.name}.NoWsdlSOAP12"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>
  	
    <InstallApp earName="${test.name}.MTOMSoap12App.ear"
                appName="${test.name}.MTOMSoap12"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>     	
  	
    <InstallApp earName="${test.name}.NoBTSoap12App.ear"
                appName="${test.name}.NoBTSoap12"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp>
  	
    <InstallApp earName="${test.name}.AsyncMethodsApp.ear"
                appName="${test.name}.AsyncMethods"
                localInstall="${conn.type}"
                useDefaultBindings="true"
                installRootLocation="${FVT.build.installableApps.dir}"
                installMode="${install.mode}"
                fvtRootLocation="${FVT.base.dir}">
    </InstallApp> 

  </target>
  
</project>

